# ---------------------------------------------------------------------------
# Lezgo Suite — variables de entorno
# Copia este archivo a .env.local y rellena lo que vayas a usar.
# El panel arranca sin ninguna de ellas: sirve datos de ejemplo.
# ---------------------------------------------------------------------------

# Base de datos (Neon Postgres). Sin esta variable el panel usa src/data/demo.ts.
# Usa una rama propia de este proyecto para no tocar tus otros proyectos en Neon.
DATABASE_URL=

# GoHighLevel API v2 — token de agencia (Private Integration Token).
# Scopes mínimos: contacts.readonly, contacts.write, opportunities.readonly,
# opportunities.write, locations.readonly, conversations/message.write,
# workflows.readonly, calendars.readonly
GHL_API_KEY=

# Subcuenta por defecto cuando una herramienta no recibe una explícita.
GHL_LOCATION_ID=

# Modelo del copiloto. Basta con UNA de las dos:
#
#   ANTHROPIC_API_KEY  — habla directo con la API de Anthropic. No depende de
#                        los créditos de Vercel.
#   AI_GATEWAY_API_KEY — pasa por Vercel AI Gateway. Requiere créditos
#                        comprados: los gratis devuelven "Free tier users do
#                        not have access to this model".
#
# Si ambas están definidas, gana ANTHROPIC_API_KEY.
ANTHROPIC_API_KEY=
AI_GATEWAY_API_KEY=

# Modelo del copiloto (opcional). Por defecto anthropic/claude-sonnet-5.
COPILOT_MODEL=

# Stripe — clave secreta o restringida (sk_live/rk_live). Sin ella, la
# facturación sigue saliendo del repositorio (demo o Neon).
STRIPE_SECRET_KEY=

# Tipo de cambio USD→MXN para normalizar los KPI de facturación. Sin ella, las
# facturas en USD se muestran en su moneda y quedan fuera de los totales.
STRIPE_FX_USD_MXN=
